<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.manajemeninformasi.riska.findingtutor">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".HomeTutorActivity">

        </activity>
        <activity android:name=".EditProfileTutorActivity" />
        <activity android:name=".CariMuridActivity" />
        <activity android:name=".KeahlianTutorActivity" />
        <activity android:name=".ProfileTutorActivity" />
        <activity android:name=".TambahKeahlianTutorActivity" />
        <activity android:name=".HomeMuridActivity">

        </activity>
        <activity android:name=".CariTutorActivity" />
        <activity android:name=".ProfileMuridActivity" />
        <activity android:name=".EditProfileMuridActivity"></activity>
    </application>

</manifest>